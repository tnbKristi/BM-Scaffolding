language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bbmodule' ]; then cd .. && eval "mv $currentfolder generator-bbmodule" && cd generator-bbmodule; fi

